version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${AIzaSyCf-MDWDOOfyBF2K6BNQptC1bqTYgUC9DA}
      - PORT=3000
    restart: unless-stopped
